import { styled } from 'stitches.config'

const PagePadding = styled('div', {
  padding: '0 5rem',
  '@tablet': { padding: '0 3rem' },
  '@mobileLandscape': { padding: '0 1rem' },
})

export default PagePadding
